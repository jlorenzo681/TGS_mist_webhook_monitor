# Mist Webhook Monitor Environment Configuration
# Copy this file to .env and update with your values

# ============================================================================
# Node Server Settings
# ============================================================================
NODE_HOSTNAME=localhost
NODE_SESSION_SECRET=3RMUqsJrX1orvJNBAlrA0KyLqD3fI7/BgiDZ8c8eAto=
NODE_PORT_HTTP=3000
NODE_HTTPS=false
NODE_PORT_HTTPS=3443

# If using HTTPS, specify certificate file names (must be in src/certs/ directory)
# NODE_HTTPS_CERT=default.pem
# NODE_HTTPS_KEY=default.key

# ============================================================================
# Webhook Collector Settings
# ============================================================================
# If server has different public IP/FQDN for webhook collector
NODE_WEBHOOK_HOSTNAME=

# Public facing port (if behind reverse proxy or NAT)
NODE_WEBHOOK_PORT=

NODE_WEBHOOK_NAME=webhook.mist-lab.fr
NODE_WEBHOOK_HTTPS=false
NODE_WEBHOOK_CERT_CHECK=false

# ============================================================================
# WebSocket Settings
# ============================================================================
# If behind reverse proxy, define the listening port
NODE_WEBSOCKET_PORT=

NODE_WEBSOCKET_SECURE=false

# ============================================================================
# MongoDB Settings
# ============================================================================
MONGO_HOST=mongodb
MONGO_DB=webhook_mon
MONGO_USER=mongo_user
MONGO_PASSWORD=mongo_password

# Generate encryption keys with:
# openssl rand -base64 32
# openssl rand -base64 64
MONGO_ENC_KEY=
MONGO_SIG_KEY=

# ============================================================================
# SMTP Settings (Optional)
# ============================================================================
SMTP_HOSTNAME=
SMTP_PORT=25
SMTP_SECURE=false
SMTP_REJECT_UNAUTHORIZED=true
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_NAME=Webhook Mist App
SMTP_FROM_EMAIL=wi-fi@corp.org
SMTP_LOGO_URL=https://cdn.mist.com/wp-content/uploads/logo.png

# ============================================================================
# Application Settings
# ============================================================================
APP_DISCLAIMER=
APP_GITHUB_URL=
APP_DOCKER_URL=
